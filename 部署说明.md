# 每日任务规划 · 手机/电脑同步部署说明

这个应用是一个单文件网页（`index.html`）。要让它：

1. **手机上也能打开** → 部署到免费托管（GitHub Pages）
2. **手机和电脑数据互通** → 用免费的 [Supabase](https://supabase.com) 做云同步

全程免费，大约 10 分钟。

---

## 一、准备 Supabase（云同步后端）

1. 打开 https://supabase.com ，注册/登录（可用 GitHub 一键登录）。
2. 点 **New project**：
   - Name 随便起，例如 `my-todo`
   - 设置一个数据库密码（随便记一下，后面用不到）
   - Region 选 **Singapore** 或离你近的
3. 等 1~2 分钟项目初始化完成。
4. 左侧菜单点 **SQL Editor** → **New query**，把 `supabase-setup.sql` 里的内容**全部粘贴**进去，点 **Run**（绿色按钮）。
5. 左侧菜单点 **Project Settings** → **API**，复制下面两个值备用：
   - `Project URL`（形如 `https://xxxxxxxx.supabase.co`）
   - `anon` + `public` 那一行的 key（形如 `eyJhbGciOi…` 很长一串）

> 这两个值先贴到记事本里，第三步会用到。

---

## 二、部署网页（让手机也能打开）

### 方式 A：GitHub Pages（推荐，免费、稳定）

1. 打开 https://github.com 注册/登录。
2. 右上角 **+** → **New repository**：
   - 名字：`task-planner`
   - 选 **Public**
   - 勾选 **Add a README file**
   - 点 **Create repository**
3. 进入仓库，点 **Add file** → **Upload files**，把 `index.html` 拖进去，点 **Commit changes**。
4. 点仓库顶部 **Settings** → 左侧 **Pages**：
   - Source 选 **Deploy from a branch**
   - Branch 选 `main`，目录选 `/(root)`
   - 点 **Save**
5. 等 1 分钟左右，回到 Pages 页面会看到一个网址：
   `https://你的用户名.github.io/task-planner/`
   —— 这个网址**手机和电脑都能打开**。

### 方式 B：Netlify Drop（更简单，几秒）

1. 打开 https://app.netlify.com/drop
2. 把整个 `task-planner` 文件夹拖进网页，几秒后得到网址。

---

## 三、配置同步（手机 + 电脑都要做一次）

1. 手机和电脑分别打开第二步得到的网址。
2. 点右上角 **⚙️** 按钮，填写：
   - **Supabase 网址**：第一步复制的 `Project URL`
   - **anon 公开密钥**：第一步复制的 anon key
   - **同步密钥**：自己随便设一个（例如 `my-todo-2026`），**两台设备必须填一模一样**
3. 点 **保存并启用同步**。顶部状态点变成绿色「已同步」即成功。

之后在一台设备添加/完成任务，另一台设备会在 **3 秒内自动同步**。

> 也可以先在电脑本地双击 `index.html` 打开测试同步——同步功能在本地文件也能用，不一定要先部署。

---

## 四、防止项目被自动暂停（保活，重要）

Supabase 免费版有个坑：**项目闲置满 7 天会被自动暂停**，暂停后所有同步请求都会失败。
暂停**不是删除**，数据还在，但必须手动恢复才能继续用。

### 4.1 手动恢复（项目已经暂停时）

1. 打开 https://supabase.com/dashboard ，点开你的项目
2. 页面上会有 **Project is paused** 提示，点 **Restore project**（或 Unpause）
3. 等 1~2 分钟，回到应用刷新，同步即恢复

### 4.2 自动保活（一劳永逸，推荐）

用 GitHub Actions 每天定时访问一次项目接口，让它永远不进入闲置状态。
**跑在 GitHub 云端，跟你自己的电脑开不开机无关。**

1. 在仓库里新建 `.github/workflows/supabase-keepalive.yml`（GitHub 网页点 **Add file → Create new file**，文件名里带 `/` 会自动建文件夹）。文件内容已生成在本地：
   `task-planner\.github\workflows\supabase-keepalive.yml`，直接全选复制粘贴即可。

2. **配置 anon key（必做）**。Supabase 现在要求所有接口都带密钥，不带一律返回 401。
   到仓库 **Settings → Secrets and variables → Actions → New repository secret**：
   - **Name** 填：`SUPABASE_ANON_KEY`
   - **Secret** 填：你项目的 **anon public key**
     （打开应用点 ⚙️，「anon 公开密钥」那一框里就有；或去 Supabase 控制台 **Project Settings → API** 复制）

   > 为什么存成 Secret 而不是直接写进文件？因为仓库是**公开**的，写进去会被爬虫扫到；
   > 而建表脚本里的 RLS 策略是 `using(true)`（全开），拿到 key 就等于拿到你的任务清单。
   > 存成 Secret 后，GitHub 还会在运行日志里自动把它打码。

3. 到仓库 **Settings → Actions → General** → 拉到最下 **Workflow permissions**，
   选 **Read and write permissions**，点 Save。（不选的话，每月的"心跳提交"会被拒绝）

4. 到 **Actions** 标签页 → 左栏选「Supabase 保活」→ **Run workflow** 手动跑一次，看到绿勾即成功。

跑失败时 GitHub 会给你发邮件，日志里会直接写明原因：

| 日志里的 HTTP | 含义 |
|---|---|
| `200` | ✅ 正常 |
| `540` | 项目被暂停了，去 Supabase 控制台点 Restore |
| `401` / `403` | Secret `SUPABASE_ANON_KEY` 没配，或填的不是 anon public key |
| `404` | 表 `sync_state` 不存在，去 SQL Editor 重跑 `supabase-setup.sql` |

> **不想用 GitHub 的话**，最简单的替代办法：**每周打开一次这个网页应用**。
> 应用一打开就在轮询，本身就是一次有效访问，能重置闲置计时。

---

## 常见问题

**Q：顶部一直显示「同步失败」？**

先看 ⚙️ 下面那行写的**具体文字**，对症处理：

| 提示里包含 | 原因 | 怎么办 |
|---|---|---|
| `Failed to fetch` | 请求没拿到合法响应（网络/代理拦截，或响应缺少 CORS 头） | 见下一问 |
| `HTTP 540` / `Project paused` | **项目被暂停了** | 见「四、保活」的 4.1 手动恢复 |
| `HTTP 404` | 表没建出来 | 回到第一步，在 SQL Editor 重跑 `supabase-setup.sql` |
| `HTTP 401` / `403` | anon key 填错，或建表脚本里的 RLS 策略没执行 | 重新复制 key；重跑 SQL |
| `HTTP 400` | 网址格式不对 | 确认是 `https://xxxx.supabase.co`，无多余路径和结尾斜杠 |

---

**Q：显示 `Failed to fetch`，但我明明能上网？**

`Failed to fetch` 是**浏览器的话术，不代表网络不通**。它最常见的成因是：
服务器确实返回了响应，但该响应**没有跨域许可（CORS）头**，被浏览器按规则拒收。

排查顺序：

1. **先确认项目没被暂停** —— 九成是这个。Supabase 暂停后返回 `HTTP 540`，
   而这个响应**不带 CORS 头**，浏览器一律翻译成 `Failed to fetch`。
   处理办法见「四、保活」的 4.1。
2. 确认两台设备都在正常网络下（公司网络、校园网、部分公共 WiFi 可能拦截境外域名）。
3. 如果本机或路由器挂了代理 / VPN 工具：某些线路会拦截特定域名或频繁掉线。
   **换一个稳定节点再试**，避开「自动选择」「负载均衡」这类会随机漂移的选项——
   它们会让同一分钟内的请求时通时不通，表现极具迷惑性。
4. 浏览器插件（广告拦截、隐私防护类）偶尔也会拦，可用无痕窗口复测。

> **中国大陆用户注意**：`*.supabase.co` 直连通常不通，必须依赖可用的代理线路。
> 关闭代理并不能解决问题，只会让情况从「时通时不通」变成「完全不通」。

**Q：两台设备数据没合并，互相覆盖了？**
- 确认两台设备的「同步密钥」填得完全一致。
- 首次连接时，以版本号更高的那台为准。

**Q：想换设备 / 清空云端数据？**
- 在 Supabase 控制台的 **Table Editor** 里打开 `sync_state` 表，删除对应行即可。

---

## 安全说明（请务必读）

- 同步通过 Supabase 的公开接口（anon key）读写，**anon key 就写在网页源码里，是对外公开的**。
- 「同步密钥」只是把数据隔离到不同「房间」的标识，**不是真正的密码**。请避免在里面记录敏感信息（密码、银行卡等）。
- 如果以后想要真正的账号登录（更安全、支持多人各自独立数据），可以在 Supabase 开启 Auth，我可以帮你加登录功能。

---

## 数据说明

- **未配置同步**：应用完全离线工作，数据存在浏览器本地（localStorage），换设备/清缓存会丢。
- **配置同步后**：本地仍会缓存一份，断网也能用；恢复网络后自动同步到云端。
- 本地数据和云端是「同一份 + 版本号」，谁的版本新就采用谁（last-write-wins）。
